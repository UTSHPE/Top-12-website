# Supabase schema

Ground truth for the `public` schema behind the Top 12 app.

**How this was produced.** The `SUPABASE_SERVICE_ROLE_KEY` in `.env.local` is
currently rejected by the project (`401 Invalid API key`), and PostgREST only
exposes its OpenAPI spec to `service_role`. Everything below was therefore
verified with the **anon** key by probing each column individually — requesting
a column that does not exist returns Postgres error `42703`, which is a reliable
existence check.

That method proves **which columns exist**. It cannot see constraints, foreign
keys, nullability, defaults, or RLS policies. Those are marked
_unverified_ below. To fill them in, run `docs/migrations/000_introspect.sql`
in the Supabase SQL editor and paste the output back here.

Last verified: 2026-08-03.

---

## Tables

There are exactly three tables in `public`. PostgREST returns `PGRST205` for
`attendance`, `check_ins`, `checkins`, `users`, and `officers` — and helpfully
suggests `sign_ins` / `members` as the nearest matches. **There is no separate
attendance table; `sign_ins` is it.**

### `events`

| Column | Notes |
| --- | --- |
| `id` | PK. UUID — `recurrence_group_id` is generated with `crypto.randomUUID()` and events are addressed by string id throughout the app. _Type unverified._ |
| `created_at` | |
| `title` | Event name shown to members. |
| `location` | |
| `event_type` | Free text; `lib/events.ts` maps it onto three visual categories. See `EVENT_TYPES`. |
| `created_by_officer` | Display name of the officer who created it. |
| `access_code` | The 6-character check-in code. Generated by `lib/accessCode.ts`. |
| `base_points` | |
| `multiplier` | Points earned = `base_points × multiplier`. |
| `recurrence_group_id` | Links a recurring series; `null` for one-offs. |
| `calendar_start` | When the event actually runs — shown to members. |
| `calendar_end` | |
| `check_in_start` | When the access code starts working. **Distinct from the calendar window.** |
| `check_in_end` | When the code stops working. |

Added by migrations in this repo (not yet applied unless you ran them):

| Column | Migration | Notes |
| --- | --- | --- |
| `is_open` | `003` | Manual kill switch for check-in. `true` by default. |
| `deleted_at` | `004` | Soft delete. `null` = live. |

Confirmed **absent** before migrations: `is_open`, `checkin_closes_at`,
`deleted_at`, and any Google Calendar id column. The calendar integration is
fire-and-forget — `app/actions/createEvent.ts` inserts to Google but stores no
reference back, so a calendar entry cannot be located from an event row.

### `members`

| Column | Notes |
| --- | --- |
| `id` | PK. |
| `created_at` | |
| `first_name` | |
| `last_name` | |
| `email` | |
| `eid` | UT EID. Unique _(unverified)_. The join key for `sign_ins`. |
| `major` | |
| `position` | Defaults to `'Member'`; officers hold elevated values. |
| `Class` | **Case-sensitive** — quoted identifier in Postgres. |

> **CLAUDE.md is wrong about this table.** It documents a `DOB` column. `DOB`
> **does not exist** (`42703`). `Class` does exist and is case-sensitive as
> documented.

Members never log in. Officer accounts live in Supabase Auth; `lib/officer.ts`
enriches the signed-in auth user from this roster.

### `sign_ins`

The attendance table.

| Column | Notes |
| --- | --- |
| `id` | PK. |
| `created_at` | Effectively the check-in timestamp. |
| `eid` | The member's EID. FK to `members.eid` — **verified**, see below. |
| `event_id` | References `events.id`. _Cascade behavior unverified; see migration `002`._ |
| `points_earned` | Denormalized `base_points × multiplier` at time of check-in, so later edits to an event don't retroactively change awarded points. |

Added by migrations in this repo:

| Column | Migration | Notes |
| --- | --- | --- |
| `deleted_at` | `004` | Stamped when the parent event is soft-deleted. |

**On `sign_ins.eid`:** there IS a foreign key, `sign_ins_eid_fkey`, pointing at
`members.eid`. **Verified 2026-08-28** by attempting a `service_role` insert
with an EID that is not on the roster:

```
23503  insert or update on table "sign_ins" violates foreign key constraint
       "sign_ins_eid_fkey"
       Key (eid)=(…) is not present in table "members".
```

An earlier version of this file inferred the opposite from `lib/leaderboard.ts`,
which falls back to the raw EID "so an orphaned sign-in still shows up". That
fallback is defensive, not evidence — the database will not accept an unknown
EID at all, so `performCheckIn`'s roster check is a second line of defence
rather than the only one.

Practical consequence: `sign_ins` can be joined to `members` without worrying
about unmatched rows, which is what `/admin/rtc` relies on.

---

## Row-level security

**Verified:** RLS is **enabled on `sign_ins`**. An anon `INSERT` returns:

```
42501  new row violates row-level security policy for table "sign_ins"
```

**Verified:** anon `SELECT` on `events`, `members`, and `sign_ins` all return
`200` with an empty array. This is ambiguous — it means either the tables are
empty or RLS is filtering every row. It cannot be resolved without working
`service_role` credentials.

**Unverified:** the actual policy definitions on all three tables, and whether
RLS is enabled on `events` and `members` at all.

This is worth resolving. `members` holds names, emails, and student EIDs; if
RLS is off there, the public anon key can read the entire roster.

## Access patterns

Server-side reads use `createAdminClient()` (`lib/supabase/admin.ts`), which
holds the service-role key and bypasses RLS. It must only ever be called from
Server Components, server actions, or route handlers — never from a client
component, and the key must never reach a `NEXT_PUBLIC_` variable.

The browser client (`lib/supabase/client.ts`) uses the anon key and is used
only for officer auth.

Because every read goes through the service-role client, **an invalid service
role key makes the whole app render as empty rather than erroring** — the query
helpers all use `data ?? []` and discard the error. If the events grid,
leaderboard, and officer dashboard are all simultaneously blank, check the key
before looking anywhere else.
